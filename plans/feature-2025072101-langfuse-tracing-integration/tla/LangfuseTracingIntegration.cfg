SPECIFICATION Spec
CONSTANTS
    MAX_BUFFER_SIZE = 10
    MAX_TRACES = 100
    OP_TYPES = {"LLM", "API", "AGENT", "WORKFLOW"}
    TRACE_LEVELS = {"DEBUG", "INFO", "WARN", "ERROR"}

INVARIANT Safety

\* Model checking constraints to limit state space
